name: Deploy Para Ambiente de Stage

on:
  push:
    branches:
      - stage   

jobs:
  deploy-stage:
    runs-on: [self-hosted, stage-runner]

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Build da imagem Docker
        working-directory: ./src
        run: docker build -t app-form-web .

      - name: Parar e remover container antigo (se existir)
        run: |
          docker stop app-form-web || true
          docker rm app-form-web || true

      - name: Subir novo container de Stage
        run: |
          docker run -d --name app-form-web -p 8080:80 app-form-web:latest

      - name: Cleanup imagens antigas
        run: docker image prune -f

